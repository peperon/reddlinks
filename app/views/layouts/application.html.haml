%html
  %head
    %title= Reddlinks
    %meta(charset="utf-8")

    = stylesheet_link_tag :application
    = javascript_include_tag :application
    = csrf_meta_tags
  %body
    #header= @page_title || "Reddlinks"
    #columns
      #side
        - if !session[:user_id]
          = render "login"
        - else
          = render "side"
      #content
        = yield
